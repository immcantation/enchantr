pipelines:
  default:
    - step:
        name: Test
        image:
            name: immcantation/test:devel 
        script:
          - sed -i '/test/ d' .Rbuildignore
          - Rscript tests/setup/install_dep.R
          - R CMD build .
