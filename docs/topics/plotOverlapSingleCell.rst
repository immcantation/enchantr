plotOverlapSingleCell
---------------------

**dups <- findSingleCellDuplicates(db, groups=“sample_id”) counts <-
singleCellSharingCounts(dups) plotOverlapSingleCell(counts)**

Description
~~~~~~~~~~~

dups <- findSingleCellDuplicates(db, groups=“sample_id”) counts <-
singleCellSharingCounts(dups) plotOverlapSingleCell(counts)

Usage
~~~~~

::

   plotOverlapSingleCell(dup_count)
